# Mode debug (1=activé, 0=désactivé)
DEBUG=1

# Clé secrète Django (à remplacer en production)
SECRET_KEY=change-me

# Hôtes autorisés, séparés par des virgules
ALLOWED_HOSTS=127.0.0.1,localhost

# Paramètres base de données (préparation future)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
