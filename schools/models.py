from django.db import models

# Réserve d'architecture (future multi-tenancy) :
# cette app est candidate pour porter l'entité "school" (tenant logique).
# Aucun modèle n'est ajouté pour éviter toute complexité active prématurée.
